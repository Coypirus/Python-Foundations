# Anonymous functions - lambda
# Key Points:
#   No name
#   One expression
#   Any number of arguments


# Lambda function doesn't have a name,
# Instead we assign it(as an object) to a variable.

f = lambda a, b: a + b

result = f(5, 6)

print(result)